@using Perplex.ContentBlocks.Rendering;
@model IContentBlockViewModel<Quote>
@{
    var content = Model.Content;
}
<section class="wrapper style3">
    <div class="inner">
        <header class="align-center">
            <h2>@content.QuoteText</h2>
            <p>@content.Citation</p>
        </header>
    </div>
</section>